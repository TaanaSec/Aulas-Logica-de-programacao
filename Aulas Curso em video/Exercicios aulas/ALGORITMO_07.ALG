algoritmo "algoritmo_07"

  
var

   angulo, seno : real

inicio

      escreva("Informe um angulo: ")
      leia(angulo)
      seno <- sen(GraupRad(angulo))
      escreva("O seno de", angulo, " é igual a", seno)

fimalgoritmo